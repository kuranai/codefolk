# Codefolk fixture

> A warm theme should remain clear in **light** and _dark_ environments.

Use [`semanticHighlighting`](https://code.visualstudio.com/api/language-extensions/semantic-highlight-guide)
alongside TextMate scopes:

```elixir
{:ok, theme} = Codefolk.load(:dark)
```
